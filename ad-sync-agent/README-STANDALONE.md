# AD Sync Agent - Standalone .exe

## 🎯 Для администраторов (конечных пользователей)

### Если у вас есть готовый AD-Sync-Agent.exe:

1. **Скопируйте файлы:**
   - `AD-Sync-Agent.exe` - запустите этот файл
   - `ad_sync_agent.py` - должен быть в той же папке

2. **Запустите AD-Sync-Agent.exe:**
   - Двойной клик на файл
   - Откроется окно с формой

3. **Заполните настройки:**
   - AD Сервер: `ldap://ваш-сервер:389`
   - Пользователь: `ваша_учетка@домен.local`
   - Пароль: `ваш_пароль`
   - Base DN: `DC=домен,DC=local`
   - URL платформы: `https://ваша-платформа.com`
   - Tenant ID: `ваш_id` (из настроек платформы)
   - API Ключ: `ваш_ключ` (из настроек AD Sync)

4. **Сохраните и запустите:**
   - Нажмите "Сохранить настройки"
   - Нажмите "Запустить агента"
   - Готово! Агент работает!

**ВАЖНО:** Standalone .exe НЕ требует установки Python! Все зависимости включены в .exe файл!

---

## 🔧 Для разработчиков (сборка .exe)

### Создание standalone .exe файла:

1. **Запустите:**
   ```
   build-standalone-exe.bat
   ```

2. **Скрипт автоматически:**
   - Проверит Python
   - Установит зависимости
   - Установит PyInstaller
   - Соберёт standalone .exe

3. **Результат:**
   - Файл: `dist\AD-Sync-Agent.exe`
   - Размер: ~15-30 МБ (все зависимости включены)
   - Не требует Python для запуска!

### Что включено в .exe:

- ✅ Tkinter (GUI интерфейс)
- ✅ ldap3 (для работы с AD)
- ✅ requests (для API)
- ✅ python-dotenv (для .env файлов)
- ✅ Все необходимые библиотеки

### Распространение:

После сборки вы можете:
- Скопировать `dist\AD-Sync-Agent.exe` в любую папку
- Скопировать `ad_sync_agent.py` в ту же папку
- Раздать администраторам - они просто запустят .exe!

---

## ❓ Проблемы?

### Ошибка "Python не найден" при запуске build-standalone-exe.bat:
- Установите Python с https://www.python.org/downloads/
- При установке отметьте "Add Python to PATH"

### Ошибка при сборке:
- Запустите build-standalone-exe.bat от имени администратора
- Проверьте подключение к интернету (нужен для установки зависимостей)

### Ошибка "ad_sync_agent.py не найден" при запуске .exe:
- Убедитесь, что `ad_sync_agent.py` находится в той же папке, что и .exe файл
- Переместите оба файла в одну папку

